version: 2.1
orbs:
  node: circleci/node@4.1.0

jobs:
  build:
    machine:
      enabled: true
    steps:
      - add_ssh_keys:
          fingerptints:
            - '93:b8:a6:3f:68:2f:4a:11:3b:45:06:85:a3:72:64:09'
      - checkout
      - run:
          name: copy nodes from Github Repo to Ec2 Instance...
          command: sudo git-clone https://github.com/jitendrasharmartm/HelloWord
          background: true
workflows:
  build_and_test:
    jobs:
      - build
  # node-tests:
  #   jobs:
  #     - node/tests